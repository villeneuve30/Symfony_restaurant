{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
    <div class="example-wrapper">
        <h1>Hello {{ controller_name }}! (Equipe)✅</h1>
        <table class="table table-hover">
            <tr>
                <td>Id</td>
                <td>Username</td>
                <td>Email</td>
                <td>Firstname</td>
                <td>Lastname</td>
                <td>Job title</td>
                <td>enabled</td>
                <td>Created at</td>
                <td>Updated at</td>
            </tr>
        {% for user in all_users %}
            <tr>
                <th scope="row">{{ user.id}}</th>
                <td>{{ user.username }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.firstname }}</td>
                <td>{{ user.lastname }}</td>
                <td>{{ user.jobTitle }}</td>
                <td>{{ user.enabled }}</td>
                <td>{{ user.createdAt | date("d/M/Y H:i")}}</td>
                <td>{{ user.updatedAt | date("d/M/Y H:i")}}</td>
            </tr>
        {% endfor %}
        </table>
    </div>
{% endblock %}
